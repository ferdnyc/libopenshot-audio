This directory can be used to store optional components of the build. The CMake configuration
will look for them and automatically activate those features. The following paths are
automatically recognized:

thirdparty/asiosdk/  -- the Steinberg ASIO SDK distribution (any version)
